# Login-Form-with-React-State-Management
Created a simple login form component in React that includes fields for username and password. Used the useState hook to manage form data locally. When the user types in the input fields, update the state in real time. Implement a submit button that, when clicked, logs the entered username and password to the console.
